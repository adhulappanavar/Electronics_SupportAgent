# Simple LanceDB Test - curl commands

# Replace YOUR_N8N_WEBHOOK_URL with your actual n8n webhook URL
# Example: https://your-n8n-instance.com/webhook/test_lancedb
# Note: The workflow now uses 127.0.0.1:8000 to avoid IPv6 connection issues

# Test 1: Basic query
curl -X POST "YOUR_N8N_WEBHOOK_URL" \
     -H "Content-Type: application/json" \
     -d '{"query": "Samsung TV issue"}'

# Test 2: Different query
curl -X POST "YOUR_N8N_WEBHOOK_URL" \
     -H "Content-Type: application/json" \
     -d '{"query": "power problem"}'

# Test 3: Empty query (should use default)
curl -X POST "YOUR_N8N_WEBHOOK_URL" \
     -H "Content-Type: application/json" \
     -d '{}'

# Expected response format:
# {
#   "success": true,
#   "query": "Samsung TV issue",
#   "lancedb_response": {
#     "found": true/false,
#     "answer": "...",
#     "confidence": 0.7,
#     "source_type": "manual",
#     "metadata": {...}
#   },
#   "timestamp": "2025-01-01T12:00:00.000Z"
# } 